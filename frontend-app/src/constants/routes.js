const LOGIN_ROUTE = "/login";

const HABILITAR_VOTO_ROUTE = "/habilitar-voto"

const LEGAJO_ROUTE = "/legajo"

const REGISTRAR_CONSTANCIA_ROUTE = "/constancia/:id"

const EMITIR_VOTO_ROUTE = "/emitir-voto"

const RESULTADOS_ROUTE = "/resultados"

const UNKNOWN_ROUTE = "*"

const routes = {
    LOGIN_ROUTE,
    HABILITAR_VOTO_ROUTE,
    LEGAJO_ROUTE,
    REGISTRAR_CONSTANCIA_ROUTE,
    EMITIR_VOTO_ROUTE,
    RESULTADOS_ROUTE,
    UNKNOWN_ROUTE,
}

export default routes;